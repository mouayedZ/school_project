import { LoginTestDirective } from './login-test.directive';

describe('LoginTestDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginTestDirective();
    expect(directive).toBeTruthy();
  });
});
